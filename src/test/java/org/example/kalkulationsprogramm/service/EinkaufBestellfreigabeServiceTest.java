package org.example.kalkulationsprogramm.service;
import static org.junit.jupiter.api.Assertions.*;import static org.mockito.Mockito.*;import com.fasterxml.jackson.databind.ObjectMapper;import org.example.kalkulationsprogramm.repository.*;import org.example.kalkulationsprogramm.service.einkauf.*;import org.junit.jupiter.api.Test;import org.springframework.web.server.ResponseStatusException;
class EinkaufBestellfreigabeServiceTest {
 @Test void nimmtKeinNichtBestellVersandereignisAn(){var orders=mock(EinkaufBestellungRepository.class);var service=new EinkaufBestellfreigabeService(orders,mock(BestellungRevisionRepository.class),mock(AngebotVersionRepository.class),mock(EinkaufKommunikationVorschauRepository.class),mock(EinkaufVorlagenService.class),mock(EinkaufPdfService.class),mock(EinkaufDateiService.class),mock(EinkaufOutboxService.class),mock(EinkaufVersandWorker.class),mock(EinkaufVersandauftragRepository.class),mock(EinkaufMengenService.class),mock(EinkaufBedarfRepository.class),mock(LieferantDokumentRepository.class),mock(EinkaufAuditService.class),new ObjectMapper(),mock(EinkaufZeugnisService.class));var event=new org.example.kalkulationsprogramm.dto.Einkauf.EinkaufVersandDto.EinkaufVersandAngenommen(java.util.UUID.randomUUID(),1L,"ANFRAGE",2L,3L,4L,java.time.Instant.now());assertThrows(ResponseStatusException.class,()->service.versandAngenommen(event));verifyNoInteractions(orders);}
}
